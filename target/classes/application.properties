spring.application.name=simple-docker-webapp
server.port=8080

# Actuator exposes /actuator/health - useful for Docker/Compose/K8s healthchecks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
